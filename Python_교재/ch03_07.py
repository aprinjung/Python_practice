alphabet = ['a', 'b', 'c','d']
print(alphabet+alphabet)
# print((alphabet.remove(alphabet)))
print(alphabet.append(alphabet))
# ValueError: list.remove(x): x not in list
# append() : 리스트만 사용가능, 리스트에 요소를 추가하는 동작을 수행하고,
# 별도의 반환 값(return value)을 갖지 않음